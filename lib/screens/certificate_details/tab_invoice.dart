import '../../general_exports.dart';

class InvoiceTab extends StatelessWidget {
  const InvoiceTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('InvoiceTab'),
    );
  }
}
